#!/bin/bash
gradle dependencies --build-file=$1 --configuration compile | grep "\-\-\- " | sed 's/^.*\-\-\- //' | sed 's/:[^:]* -> /:/'| sed 's/[(][*][)]//' | sed 's/ *$//'| grep "project " |sed '/^\n$/d' | sed 's/^project ://' | sort | uniq 